server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/mieandu/microservice/
          search-paths: config-repo
      discovery:
        enabled: true
      label: nokafka
